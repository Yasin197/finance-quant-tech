fn sub(a: i32, b:i32) -> i32 {
    a -b
}


fn main() {
    let sum = 2 + 2;
    let value = 10 -5;
    let division = 10 / 2;
    let mult = 5 * 5;

    let five = sub(8, 3);
    

    // this is using % which is remainder division, this can help determine if a number is odd or even
    // here we get 0 because 3 goes into 6 even number of times
    let rem = 6 % 3;

    // here we get 2
    let rem2 = 6 % 4;

}